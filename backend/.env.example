# Model settings
MODEL_PATH=../data/models/model.onnx

# Server settings
HOST=0.0.0.0
PORT=8000

# CORS
CORS_ORIGINS=*

# Supabase (optional — leave blank to skip DB storage)
SUPABASE_URL=
SUPABASE_ANON_KEY=
